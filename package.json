{
  "name": "yttracker",
  "version": "2.0.1",
  "author": "Thomas Couchoud <zerderr@gmail.com> (https://github.com/RakSrinaNa/)",
  "description": "This extension will track your time on YouTube",
  "homepage": "https://github.com/RakSrinaNa/YTTracker/blob/master/README.md",
  "bugs": "https://gitlab.com/RakSrinaNa/YTTracker/issues",
  "repository": "github:RakSrinaNa/YTTracker",
  "license": "GNU GPLv3",
  "private": true,
  "files": [
    "/chrome",
    "/firefox",
    "/includes"
  ],
  "scripts": {
    "eslint": "eslint . --ext .json --ext .js",
    "test": "npm run eslint",
    "clean": "rm -R temp",
    "init": "mkdir temp",
    "includes": "cp -R includes/* temp",
    "chromeInit": "npm run init && cp -R chrome/* temp",
    "chromeManifest": "cd temp && mv manifestProd.json manifest.json && replace-in-file !!!VERSION!!! ${npm_package_version} manifest.json && cd ../",
    "chromePackage": "npm run includes && cd temp && zip -r chromeExtension.zip ./* -x \"*.DS_Store\" && cd ../ && mv temp/chromeExtension.zip chromeExtension.zip",
    "chromeBuild": "npm run chromeInit && npm run chromeManifest && npm run chromePackage && npm run clean",
    "chromeRelease": "npm run chromeBuild && npm run chromeSend && npm run chromeClear",
    "chromeSend": "webstore upload --file chromeExtension.zip --source chromeExtension.zip --extension-id knnlnielflnfhdohmihofhdelgahgjdb --client-id ${CLIENT_ID} --client-secret ${CLIENT_SECRET} --refresh-token ${REFRESH_TOKEN} --auto-publish",
    "chromeClear": "rm chromeExtension.zip",
    "firefoxInit": "npm run init && cp -R firefox/* temp",
    "firefoxManifest": "cd temp && mv manifestProd.json manifest.json && replace-in-file !!!VERSION!!! ${npm_package_version} manifest.json && cd ../",
    "firefoxPackage": "npm run includes && cd temp && zip -r firefoxExtension.xpi ./* -x \"*.DS_Store\" && cd ../ && mv temp/firefoxExtension.xpi firefoxExtension.xpi",
    "firefoxBuild": "npm run firefoxInit && npm run firefoxManifest && npm run firefoxPackage && npm run clean",
    "firefoxClear": "rm firefoxExtension.xpi"
  },
  "devDependencies": {
    "chrome-webstore-upload-cli": "^1.2.0",
    "eslint": "^6.5.1",
    "eslint-plugin-json": "^1.4.0",
    "eslint-plugin-no-unsanitized": "^3.0.2",
    "xo": "^0.25.3",
    "replace-in-file": "^4.1.3"
  },
  "xo": {
    "envs": [
      "browser"
    ]
  },
  "eslintConfig": {
    "plugins": [
      "no-unsanitized",
      "json"
    ],
    "parserOptions": {
      "ecmaVersion": 6
    },
    "env": {
      "browser": true
    },
    "extends": "eslint:recommended",
    "rules": {
      "indent": [
        "error",
        "tab",
        {
          "SwitchCase": 1
        }
      ],
      "linebreak-style": [
        "error",
        "unix"
      ],
      "quotes": [
        "error",
        "single"
      ],
      "semi": [
        "error",
        "always"
      ],
      "no-console": "off",
      "no-undef": "off",
      "no-unused-vars": "off",
      "no-empty": [
        "error",
        {
          "allowEmptyCatch": true
        }
      ]
    }
  }
}
