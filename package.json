{
  "name": "YTTracker",
  "version": "0.6.1",
  "author": "Thomas Couchoud <zerderr@gmail.com> (https://github.com/MrCraftCod/)",
  "description": "This extension will track your time on YouTube",
  "homepage": "https://chrome.google.com/webstore/detail/youtube-tracker/knnlnielflnfhdohmihofhdelgahgjdb",
  "files": [
    "/src"
  ],
  "repository" : {
    "type" : "git",
    "url" : "https://github.com/MrCraftCod/YTTracker.git"
  },
  "scripts": {
    "test": "xo src/*.js",


    "chromeManifest": "cd chrome && cp manifestProd.json manifest.json && cd ../",
    "chromeManifestBeta": "cd chrome && cp manifestTest.json manifest.json && cd ../",

    "chromeIncludes": "cp -R includes/assets chrome && cp -R includes/css chrome && cp -R includes/js chrome",
    "chromePackage": "npm run chromeIncludes && cd chrome && zip -r chromeExtension.zip ./* -x \"*.DS_Store\" && cd ../ && mv chrome/extension.zip extension.zip",

    "chromeRelease": "npm run chromeManifest && npm run chromePackage && npm run chromeSend && npm run chromeClear && npm run chromeReleaseBeta",
    "chromeReleaseBeta": "npm run chromeManifestBeta && npm run chromePackage && npm run chromeSendBeta",

    "chromeSend": "webstore upload --file extension.zip --source chromeExtension.zip --extension-id knnlnielflnfhdohmihofhdelgahgjdb --client-id $CLIENT_ID --client-secret $CLIENT_SECRET --refresh-token $REFRESH_TOKEN --auto-publish",
    "chromeSendBeta": "webstore upload --file extension.zip --source chromeExtension.zip --extension-id moboafdnejnjnppicfiadaalobjeemec --client-id $CLIENT_ID --client-secret $CLIENT_SECRET --refresh-token $REFRESH_TOKEN --auto-publish",

    "chromeClear": "rm chrome/manifest.json && rm chromeExtension.zip",


    "firefoxIncludes": "cp -R includes/assets firefox && cp -R includes/css firefox && cp -R includes/js firefox",
    "firefoxPackage": "npm run firefoxIncludes && cd firefox && zip -r firefoxExtension.xpi ./* -x \"*.DS_Store\" && cd ../ && mv firefox/firefoxExtension.xpi firefoxExtension.xpi",

    "firefoxClear": "rm firefoxExtension.xpi"
  },
  "devDependencies": {
    "chrome-webstore-upload-cli": "^0.2.0",
    "xo": "*"
  },
  "xo": {
    "envs": [
      "browser"
    ],
    "space": true,
    "rules": {
      "camelcase": 0
    }
  }
}